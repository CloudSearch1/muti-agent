# IntelliTeam 依赖配置

# 核心依赖
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# 解决requests/urllib3版本兼容性问题
urllib3>=1.26.0,<3.0.0
requests>=2.32.0

# 数据库
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# 安全
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# 安全依赖显式版本（防止间接依赖过旧）
cryptography>=46.0.5
certifi>=2026.2.25
bcrypt>=5.0.0

# 工具
httpx>=0.26.0
structlog>=24.1.0
python-dotenv>=1.0.0
tenacity>=8.2.0

# 测试
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-html>=4.0.0
pytest-timeout>=2.2.0

# 代码质量
black>=23.0.0
pylint>=3.0.0
coverage>=7.0.0

# 缓存
redis>=5.0.0

# 任务队列
celery>=5.3.0

# JWT
PyJWT>=2.8.0

# LLM（可选，根据需要安装）
openai>=1.0.0
# anthropic>=0.18.0
dashscope>=1.16.0  # 阿里云通义千问 SDK（ChatTongyi 需要）

# LangChain（ReAct Agent 需要）
langchain>=0.1.0
langchain-core>=0.1.0
langchain-openai>=0.0.5
langchain-community>=0.0.10

# CLI 和交互式工具
typer>=0.9.0
rich>=13.0.0

# Slack 集成
slack-bolt>=1.18.0
slack-sdk>=3.25.0

# Discord 集成（可选）
# discord.py>=2.3.0

# Microsoft Teams 集成（可选）
# teams-webhook-messages>=0.1.0
